
// freopen("playground/data.in", "r", stdin);

#include <ctype.h>
#include <math.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "../uthash.h"

/*
 * scanf 自动跳过空白字符；先 scanf 再 fgets(按行读函数)
 * 可能会读到空白，尽量统一使用其中一种
 */


int main(void)
{
    int a  = 1;
    int b = 2;
    int c = a + b;
}